name: Deploy App to AWS

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: copy file via ssh password
        uses: appleboy/scp-action@master
        with:
          host: ec2-13-54-221-181.ap-southeast-2.compute.amazonaws.com
          username: AKIATWA57VH5L4IVAOSX
          password: Om/aIqvPu8lnkla0nMJARDvCDUITyql+N/pqxFqr
          source: "docker-compose.yml"
          target: ""
